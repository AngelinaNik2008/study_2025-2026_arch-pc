РОССИЙСКИЙ УНИВЕРСИТЕТ ДРУЖБЫ НАРОДОВ
Факультет физико-математических и естественных наук

ОТЧЕТ ПО ЛАБОРАТОРНОЙ РАБОТЕ № 6

Студент: Николаева Ангелина Борисовна
Студенческий билет: 1032253612
Группа: НКАбд-04-25

МОСКВА 2026 г.

Содержание

    1.Цель работы

    2.Выполнение лабораторной работы
    2.1. Символьные и численные данные в NASM
    2.2. Выполнение арифметических операций в NASM
    2.3. Ответы на контрольные вопросы
    2.4. Задание для самостоятельной работы

    3.Выводы

    

Список иллюстраций

    Создание нового каталога

    Сохранение новой программы

    Запуск изначальной программы

    Измененная программа

    Запуск измененной программы

    Вторая программа

    Вывод второй программы

    Вывод измененной второй программы

    Замена функции вывода во второй программе

    Третья программа

    Запуск третьей программы

    Изменение третьей программы

    Запуск измененной третьей программы

    Программа для подсчета варианта

    Запуск программы для подсчета варианта

    Запуск и проверка программы

    1.Цель работы

Цель данной лабораторной работы - освоение арифметических инструкций языка ассемблера NASM.

    2.Выполнение лабораторной работы

2.1. Символьные и численные данные в NASM

Создаю каталог для программ лабораторной работы №6 и перехожу в него, создаю там файл [Рис. 2.1: Создание нового каталога] (1.png)

В созданном файле ввожу программу из листинга [Рис. 2.2: Сохранение новой программы] (2.png)

Создаю исполняемый файл и запускаю его, вывод программы отличается от предполагаемого изначально, ибо коды символов в сумме дают символ j по таблице ASCII [Рис. 2.3: Запуск изначальной программы] (3.png)

Изменяю текст изначальной программы, убрав кавычки [Рис. 2.4: Измененная программа] (4.png)

На этот раз программа выдала пустую строчку, это связано с тем, что символ 10 означает переход на новую строку [Рис. 2.5: Запуск измененной программы] (5.png)

Создаю новый файл для будущей программы и записываю в нее код из листинга [Рис. 2.6: Вторая программа] (6.png)

Создаю исполняемый файл и запускаю его, теперь отображается результат 106, программа, как и в первый раз, сложила коды символов, но вывела само число, а не его символ, благодаря замене функции вывода на iprintLF [Рис. 2.7: Вывод второй программы] (7.png)

Убрав кавычки в программе, я снова ее запускаю и получаю предполагаемый изначально результат [Рис. 2.8: Вывод измененной второй программы] (8.png)

Заменив функцию вывода на iprint, я получаю тот же результат, но без переноса строки [Рис. 2.9: Замена функции вывода во второй программе] (9.png)

2.2. Выполнение арифметических операций в NASM

Создаю новый файл и копирую в него содержимое листинга [Рис. 2.10: Третья программа] (10.png)

Программа выполняет арифметические вычисления, на вывод идет результирующее выражение и его остаток от деления [Рис. 2.11: Запуск третьей программы] (11.png)

Заменив переменные в программе для выражения f(x) = (4*6+2)/5 [Рис. 2.12: Изменение третьей программы] (12.png)

Запуск программы дает корректный результат [Рис. 2.13: Запуск измененной третьей программы] (13.png)

Создаю новый файл и помещаю текст из листинга [Рис. 2.14: Программа для подсчета варианта] (14.png)

Запустив программу и указав свой номер студенческого билета, я получил свой вариант для дальнейшей работы [Рис. 2.15: Запуск программы для подсчета варианта] (15.png)

2.3. Ответы на контрольные вопросы

    За вывод сообщения "Ваш вариант" отвечают строки кода:
    mov eax, rem
    call sprint

    Инструкция mov ecx, x используется, чтобы положить адрес вводимой строки x в регистр ecx. mov edx, 80 - запись в регистр edx длины вводимой строки. call sread - вызов подпрограммы из внешнего файла, обеспечивающей ввод сообщения с клавиатуры.

    call atoi используется для вызова подпрограммы из внешнего файла, которая преобразует ASCII-код символа в целое число и записывает результат в регистр eax.

    За вычисления варианта отвечают строки:
    xor edx, edx ; обнуление edx для корректной работы div
    mov ebx, 20 ; ebx = 20
    div ebx ; eax = eax/20, edx - остаток от деления
    inc edx ; edx = edx + 1

    При выполнении инструкции div ebx остаток от деления записывается в регистр edx.

    Инструкция inc edx увеличивает значение регистра edx на 1.

    За вывод на экран результатов вычислений отвечают строки:
    mov eax, edx
    call iprintLF

2.4. Задание для самостоятельной работы

В соответствии с выбранным вариантом, я реализую программу для подсчета функции f(x) = 10 + (31x - 5). Проверка на нескольких переменных показывает корректное выполнение программы [Рис. 2.16: Запуск и проверка программы] (16.png)

Прилагаю код своей программы:

%include 'in_out.asm'

SECTION .data
msg: DB 'Введите значение переменной x: ',0
rem: DB 'Результат: ',0

SECTION .bss
x: RESB 80

SECTION .text
GLOBAL _start

_start:
mov eax, msg
call sprint
mov ecx, x
mov edx, 80
call sread
mov eax, x
call atoi
mov ebx, 31
mul ebx
sub eax, 5
add eax, 10
mov edi, eax
mov eax, rem
call sprint
mov eax, edi
call iprintLF

    3.Выводы

При выполнении данной лабораторной работы я освоил арифметические инструкции языка ассемблера NASM.
